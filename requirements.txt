# Python >= 3.10 is required.
#
# CUDA installation order:
# 1. Install torch and torchvision from the official PyTorch CUDA index that
#    matches the NVIDIA driver (replace cu128 when another build is required):
#    python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# 2. Install this file:
#    python -m pip install -r requirements.txt

torch>=2.1,<3
torchvision>=0.16,<1

numpy>=1.24.4,<3
opencv-python-headless>=4.9.0.80,<6
albumentations>=2.0,<3
matplotlib>=3.7,<4
